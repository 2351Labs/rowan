import { Meta } from "@storybook/blocks";

import "./home.css";

<Meta title="Home" />

# Rowan

<img className="home-mark" src="brand/rowan.png" alt="" width="96" height="96" />

<div className="home-page">
  <p className="home-lede">
    A vanilla Web Component design system: Custom Elements, Shadow DOM, slots, CSS tokens, and
    ElementInternals. Version 0.6. Packages <code>@rowan-ui/core</code> and{" "}
    <code>@rowan-ui/icons</code>.
  </p>

<section className="home-why">
  <h2>Why Rowan</h2>
  <p>
    Rowan is named after the rowan tree (<em>Sorbus aucuparia</em>): small, hardy, and easy to
    recognize without being loud. It grows at edges and in thin soil. The scarlet berries are a
    detail on the tree, not the tree itself.
  </p>
  <p>That is the brief for this library.</p>
  <ul>
    <li>
      <strong>Vanilla first.</strong> Custom Elements, Shadow DOM, slots, CSS tokens. No framework
      runtime in the box.
    </li>
    <li>
      <strong>Composition over inheritance.</strong> Public API is attributes, properties, slots,
      events, and parts. Components nest; they do not subclass each other.
    </li>
    <li>
      <strong>Config where reuse matters.</strong> Tables and similar surfaces take data and cell
      config so the same component can be a checkbox column in one app and a link column in another.
    </li>
    <li>
      <strong>Quiet theming.</strong> Primitive → semantic → component tokens. Dark mode is a token
      swap, not a fork.
    </li>
  </ul>
  <p>
    The mark is an R with a single berry in the counter. The letter is the product. The berry is the
    reminder: keep the accent small.
  </p>
</section>

<div className="home-grid">
  <article className="home-card">
    <h2>Foundations</h2>
    <p>
      Token layers and shipped palettes. Start at{" "}
      <a href="?path=/docs/foundations-tokens--docs">Tokens</a> or{" "}
      <a href="?path=/docs/foundations-themes--docs">Themes</a>.
    </p>
  </article>
  <article className="home-card">
    <h2>Components</h2>
    <p>Live canvases grouped by Actions, Forms, Overlays, Navigation, and Data Display.</p>
  </article>
  <article className="home-card">
    <h2>React</h2>
    <p>
      Generated wrappers are the default.{" "}
      <a href="?path=/docs/integrations-using-rowan-from-react--docs">Using Rowan from React</a>{" "}
      shows <code>RowanButton</code> next to the raw tag.
    </p>
  </article>
  <article className="home-card">
    <h2>Icons</h2>
    <p>
      Optional package next to core. Tree-shakable SVG modules, no name registry.{" "}
      <a href="?path=/docs/foundations-icons--docs">Icons</a>.
    </p>
  </article>
  <article className="home-card">
    <h2>MapLibre</h2>
    <p>
      Optional adapter. Application-owned styles and attribution.{" "}
      <a href="?path=/docs/integrations-maplibre-map--docs">MapLibre Map</a>.
    </p>
  </article>
</div>

<pre className="home-install">{`npm install @rowan-ui/core @rowan-ui/icons
import "@rowan-ui/core";
import "@rowan-ui/core/tokens";`}</pre>

  <p className="home-note">
    <code>rowan-bar-chart</code> is experimental and out of the 0.6 surface.
  </p>
</div>
